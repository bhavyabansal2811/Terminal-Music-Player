# Terminal-Music-Player
Terminal Music Player is a command-line music player built using Node.js. It allows users to play, pause,etc. and navigate through local music files directly from the terminal. The project uses Node.js's fs module to read and manage music files locally and spawn from the child_process module to create and control the audio playback process.
